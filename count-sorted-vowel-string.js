let countSortedVowelString = function(n) {
    let result = 0;
    let vowel = ['a', 'e', 'i', 'o', 'u'];

    for(let j = `${vowel[i]}`; j < n; j++){
     result = j  
    };
    
    return result; 
};

countSortedVowelString(3);
