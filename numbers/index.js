let addTwoNumber = function(l1, l2) {
  l1  
}

let listNode = function(val, next) {
  return { 
    val: (val===undefined ? 0 : val),
    next :(next===undefined ? nul : next)
  }
}; 

addTwoNumber(listNode(), listNode())
